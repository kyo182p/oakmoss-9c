# 雲林排班工具

單一 HTML 檔的排班工具。把「人力總表」和「每日出海計畫」兩個 Excel 拖進網頁就能用。

- 所有處理都在瀏覽器裡完成，**Excel 不會上傳到任何地方**，這個網站也沒有後端。
- 原始 Excel 只會被讀取，不會被改動；存檔一律另存新檔。
- 需要 Chrome / Edge 103 以上（用到瀏覽器內建的 zip 解壓縮）。

## 更新網頁

把新的 `yunlin.html` 覆蓋成 `index.html`，commit、push 之後約一分鐘 GitHub Pages 就會更新。
